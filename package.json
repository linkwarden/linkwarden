{
  "name": "linkwarden",
  "packageManager": "yarn@1.22.0",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "web:dev": "yarn workspace @linkwarden/web dev",
    "web:build": "yarn workspace @linkwarden/web build",
    "web:start": "yarn workspace @linkwarden/web start",
    "worker:dev": "yarn workspace @linkwarden/worker dev",
    "worker:start": "yarn workspace @linkwarden/worker start",
    "format": "yarn workspaces run format"
  },
  "devDependencies": {}
}
