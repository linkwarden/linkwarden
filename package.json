{
  "name": "linkwarden",
  "packageManager": "yarn@1.22.0",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn workspace @linkwarden/web dev",
    "build": "yarn workspaces run build",
    "start": "yarn workspace @linkwarden/web start",
    "lint": "yarn workspaces run lint",
    "format": "yarn workspaces run format"
  },
  "devDependencies": {}
}
